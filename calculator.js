function calculator(oprator, left, right){
	
	// 더하기 
	if(oprator === '+')
		return left + right;

	// 빼기
	if(oprator === '-')
		return left - right;

	// 나눗셈
	if(oprator === '/')
	return left / right;

	// 곱하기

}